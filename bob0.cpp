#include <bits/stdc++.h>
using namespace std;

int main() {
  //* type variable = value;

  string name = "Bob is cool.";  // 記得加雙引號
  int age = 20;
  float star = 1.4;
  bool is_bob = true;

  //? "int" vs "long long"
  // int age;
  // INT32_MAX, INT32_MIN;

  // long long age_ll;
  // LONG_LONG_MAX, LONG_LONG_MIN;

  return 0;
}
